--- a/io/papermc/paper/FeatureHooks.java
+++ b/io/papermc/paper/FeatureHooks.java
@@ -57,9 +_,11 @@
             if (player.connection == null) continue;
 
             final Boolean shouldModify = chunk.getLevel().chunkPacketBlockController.shouldModify(player, chunk);
-            player.connection.send(refreshPackets.computeIfAbsent(shouldModify, s -> { // Use connection to prevent creating firing event
-                return new ClientboundLevelChunkWithLightPacket(chunk, chunk.getLevel().getLightEngine(), null, null, (Boolean) s);
+            final int hideY = io.canvasmc.canvas.chunk.antifreecam.AntiFreecam.hideBelowY(player);
+            player.connection.send(refreshPackets.computeIfAbsent(java.util.List.of(shouldModify, hideY), s -> { // Use connection to prevent creating firing event
+                return new ClientboundLevelChunkWithLightPacket(chunk, chunk.getLevel().getLightEngine(), null, null, shouldModify, hideY);
             }));
+            io.canvasmc.canvas.chunk.antifreecam.AntiFreecam.noteSent(player, chunk.getPos(), hideY);
             // Paper end - Anti-Xray
         }
     }
