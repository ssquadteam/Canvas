--- a/net/minecraft/world/level/levelgen/DensityFunctions.java
+++ b/net/minecraft/world/level/levelgen/DensityFunctions.java
@@ -278,13 +_,87 @@
         return new DensityFunctions.FindTopSurface(density, upperBound, lowerBound, stepSize);
     }
 
-    private record Ap2(
-        @Override DensityFunctions.TwoArgumentSimpleFunction.Type type,
-        @Override DensityFunction argument1,
-        @Override DensityFunction argument2,
-        @Override double minValue,
-        @Override double maxValue
-    ) implements DensityFunctions.TwoArgumentSimpleFunction {
+    // Canvas start - cache the hash - NoiseChunk hashes the density tree once per chunk, and a record hash walks its
+    // whole subtree to do it
+    private static final class Ap2 implements DensityFunctions.TwoArgumentSimpleFunction {
+        private final DensityFunctions.TwoArgumentSimpleFunction.Type type;
+        private final DensityFunction argument1;
+        private final DensityFunction argument2;
+        private final double minValue;
+        private final double maxValue;
+        private final int hash;
+
+        Ap2(
+            final DensityFunctions.TwoArgumentSimpleFunction.Type type,
+            final DensityFunction argument1,
+            final DensityFunction argument2,
+            final double minValue,
+            final double maxValue
+        ) {
+            this.type = type;
+            this.argument1 = argument1;
+            this.argument2 = argument2;
+            this.minValue = minValue;
+            this.maxValue = maxValue;
+
+            int hash = type.hashCode();
+            hash = (hash * 31) + argument1.hashCode();
+            hash = (hash * 31) + argument2.hashCode();
+            hash = (hash * 31) + Double.hashCode(minValue);
+            this.hash = (hash * 31) + Double.hashCode(maxValue);
+        }
+
+        @Override
+        public DensityFunctions.TwoArgumentSimpleFunction.Type type() {
+            return this.type;
+        }
+
+        @Override
+        public DensityFunction argument1() {
+            return this.argument1;
+        }
+
+        @Override
+        public DensityFunction argument2() {
+            return this.argument2;
+        }
+
+        @Override
+        public double minValue() {
+            return this.minValue;
+        }
+
+        @Override
+        public double maxValue() {
+            return this.maxValue;
+        }
+
+        @Override
+        public boolean equals(final Object other) {
+            if (this == other) {
+                return true;
+            }
+            if (!(other instanceof final DensityFunctions.Ap2 that) || this.hash != that.hash) {
+                return false;
+            }
+            return this.type == that.type
+                && Double.compare(this.minValue, that.minValue) == 0
+                && Double.compare(this.maxValue, that.maxValue) == 0
+                && this.argument1.equals(that.argument1)
+                && this.argument2.equals(that.argument2);
+        }
+
+        @Override
+        public int hashCode() {
+            return this.hash;
+        }
+
+        @Override
+        public String toString() {
+            return "Ap2[type=" + this.type + ", argument1=" + this.argument1 + ", argument2=" + this.argument2
+                + ", minValue=" + this.minValue + ", maxValue=" + this.maxValue + "]";
+        }
+    // Canvas end - cache the hash
         @Override
         public double compute(final DensityFunction.FunctionContext context) {
             double v1 = this.argument1.compute(context);
@@ -889,9 +_,76 @@
         }
     }
 
-    private record MulOrAdd(
-        DensityFunctions.MulOrAdd.Type specificType, @Override DensityFunction input, @Override double minValue, @Override double maxValue, double argument
-    ) implements DensityFunctions.TwoArgumentSimpleFunction, DensityFunctions.PureTransformer {
+    // Canvas start - cache the hash
+    private static final class MulOrAdd implements DensityFunctions.TwoArgumentSimpleFunction, DensityFunctions.PureTransformer {
+        private final DensityFunctions.MulOrAdd.Type specificType;
+        private final DensityFunction input;
+        private final double minValue;
+        private final double maxValue;
+        private final double argument;
+        private final int hash;
+
+        MulOrAdd(
+            final DensityFunctions.MulOrAdd.Type specificType,
+            final DensityFunction input,
+            final double minValue,
+            final double maxValue,
+            final double argument
+        ) {
+            this.specificType = specificType;
+            this.input = input;
+            this.minValue = minValue;
+            this.maxValue = maxValue;
+            this.argument = argument;
+
+            int hash = specificType.hashCode();
+            hash = (hash * 31) + input.hashCode();
+            hash = (hash * 31) + Double.hashCode(minValue);
+            hash = (hash * 31) + Double.hashCode(maxValue);
+            this.hash = (hash * 31) + Double.hashCode(argument);
+        }
+
+        @Override
+        public DensityFunction input() {
+            return this.input;
+        }
+
+        @Override
+        public double minValue() {
+            return this.minValue;
+        }
+
+        @Override
+        public double maxValue() {
+            return this.maxValue;
+        }
+
+        @Override
+        public boolean equals(final Object other) {
+            if (this == other) {
+                return true;
+            }
+            if (!(other instanceof final DensityFunctions.MulOrAdd that) || this.hash != that.hash) {
+                return false;
+            }
+            return this.specificType == that.specificType
+                && Double.compare(this.minValue, that.minValue) == 0
+                && Double.compare(this.maxValue, that.maxValue) == 0
+                && Double.compare(this.argument, that.argument) == 0
+                && this.input.equals(that.input);
+        }
+
+        @Override
+        public int hashCode() {
+            return this.hash;
+        }
+
+        @Override
+        public String toString() {
+            return "MulOrAdd[specificType=" + this.specificType + ", input=" + this.input + ", minValue=" + this.minValue
+                + ", maxValue=" + this.maxValue + ", argument=" + this.argument + "]";
+        }
+    // Canvas end - cache the hash
         @Override
         public DensityFunctions.TwoArgumentSimpleFunction.Type type() {
             return this.specificType == DensityFunctions.MulOrAdd.Type.MUL
